components:
  schemas:
    Credentials:
      type: object
      properties:
        username:
          type: string
          example: "foo"
        password:
          type: string
          example: "*******"
