components:
  schemas:
    Credentials:
      type: object
      properties:
        username:
          type: string
          example: "foo"
        password:
          type: string
          example: "*******"
      required:
        - username
        - password

    Credentials--extended:
      type: object
      properties:
        email:
          type: string
          example: "foo@foobar.com"
      required:
        - email
