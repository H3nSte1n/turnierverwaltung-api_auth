components:
  schemas:
    JWT:
      type: object
      properties:
        jwt:
          type: string
          example: '***.***.***'
      required:
        - jwt
    JWT-extended:
      type: object
      properties:
        role:
          type: string
          example: admin
      required:
        - role
